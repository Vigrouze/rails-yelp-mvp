<%= simple_form_for [@restaurant] do |r| %>
  <%= r.label :name %>
  <%= r.text_field :name %>
  <%= r.label :address %>
  <%= r.text_field :address %>
  <%= r.label :category %>
  <%= r.text_field :category %>
  <%= r.submit "Add your restaurant", class: "btn btn-primary" %>
<% end %>
